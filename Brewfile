# System-wide core tools
brew "chezmoi"
brew "curl"
brew "git"
brew "git-lfs"
brew "htop"
brew "openssh"
brew "ripgrep"
brew "vim"
brew "wget"

# dev-utils runtime deps
brew "gh"
brew "fzf"
brew "jq"
brew "ruby"

# shell/terminal tooling
brew "git-delta"
brew "zellij"
brew "starship"
brew "direnv"
brew "lazygit"

# terminal emulator + font
cask "alacritty"

# editor
cask "zed"

# toolchain managers (R6, R7)
brew "mise"
brew "proto"

# GUI apps previously installed via `homebrew.enable = true` cask management —
# populate the rest of this list from `brew bundle dump` at implementation time.
