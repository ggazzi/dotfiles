# Concepts

This repo's domain vocabulary lives in `CONTEXT.md` at the repo root, not here — see `docs/agents/domain.md` for how agents should consume it (along with `docs/adr/` for architecture decisions). This file exists only as a pointer for tools/skills that default to looking for `CONCEPTS.md`.
